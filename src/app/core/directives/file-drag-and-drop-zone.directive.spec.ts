import { FileDragAndDropZoneDirective } from './file-drag-and-drop-zone.directive';

describe('FileDragAndDropZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new FileDragAndDropZoneDirective();
    expect(directive).toBeTruthy();
  });
});
